# Update these URLs to match your actual environments
staging:
  internal_api_url: ${INTERNAL_API_URL}
  dashboard_url: ${DASHBOARD_URL}
  uniware_api_url: ${UNIWARE_API_URL}
  easycom_api_url: ${EASYCOM_API_URL}
  clickpost_api_url: ${CLICKPOST_API_URL}

prod:
  internal_api_url: ${INTERNAL_API_URL}
  dashboard_url: ${DASHBOARD_URL}
  uniware_api_url: ${UNIWARE_API_URL}
  easycom_api_url: ${EASYCOM_API_URL}
  clickpost_api_url: ${CLICKPOST_API_URL}
