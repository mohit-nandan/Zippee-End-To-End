[pytest]
markers =
    smoke: critical happy-path checks, runs daily
    sanity: core feature coverage, runs after every deployment
    regression: full suite, runs weekly or on-demand
    api: API layer tests
    web: Web dashboard tests
    mobile: Rider app mobile tests
addopts = --tb=short -v
